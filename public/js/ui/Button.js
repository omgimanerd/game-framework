/**
 * @fileoverview This class encapsulates a button element and is used for
 *   dynamically generating the lobby menus.
 * @author alvin.lin.dev@gmail.com (Alvin Lin)
 */

function Button(parent, id, class) {
  this.parent = parent;
  this.id = id;
  this.class = class;
}
